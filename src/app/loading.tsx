import RouteLoadingState from "@/components/layout/route-loading-state";

export default function Loading() {
  return (
    <RouteLoadingState
      title="Loading CampusLift"
      description="Preparing the latest projects, profile data, and dashboard state."
    />
  );
}
